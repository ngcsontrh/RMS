/**
 * @typedef {Object} PageData
 * @template T
 * @property {T[]} items - List of data items
 * @property {number} total - Total number of items
 */

/**
 * Pagination data model
 */
const PageData = {
  items: [],
  total: 0
};

export default PageData;